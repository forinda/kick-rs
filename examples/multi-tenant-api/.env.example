DATABASE_URL=postgres://mt:mt@localhost:5433/multi_tenant
BIND_ADDR=0.0.0.0:3001
RUST_LOG=info,multi_tenant_api=debug,kick_rs=info

# Comma-separated list of tenant slugs this app accepts. In a real
# system you'd resolve this from a tenants table, JWT claims, or a
# control-plane service; hardcoded here for demo simplicity.
TENANTS=acme,globex,initech
