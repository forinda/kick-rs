# Copy to `.env` and tweak. The example reads these via std::env at startup.

DATABASE_URL=postgres://users:users@localhost:5432/users_api
BIND_ADDR=0.0.0.0:3000
RUST_LOG=info,users_api=debug,rustkick=debug
