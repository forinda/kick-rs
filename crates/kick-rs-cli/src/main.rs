//! `cargo kick` — placeholder entry point.
//!
//! See [`SPEC.md` §7](../../SPEC.md#7-cli-cargo-kick) for the planned
//! command surface (`new`, `dev`, `g`, `add`, `info`, `check`).

fn main() {
    eprintln!("cargo kick — foundation in progress. Commands land in Phase 5.");
    eprintln!("See SPEC.md §7 for the planned surface.");
    std::process::exit(1);
}
