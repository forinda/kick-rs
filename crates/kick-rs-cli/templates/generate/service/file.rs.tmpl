//! `{{service_pascal}}` — generated by `cargo kick g service`.
//!
//! Register on the parent module's `define()` builder via
//! `.service::<{{service_pascal}}>()` after a matching `use` import.

use kick_rs::service;

/// `{{service_pascal}}` — replace this stub with the real service body.
///
/// Add `Inject<Dep>` or `Arc<Dep>` fields; the `#[service]` macro
/// rewrites them and wires each from the DI container at construction.
#[service]
pub struct {{service_pascal}} {
    // pub db: Inject<sqlx::PgPool>,
}

impl {{service_pascal}} {
    pub fn hello(&self) -> &'static str {
        "{{service_snake}} ready"
    }
}
