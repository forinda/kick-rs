//! `{{contributor_pascal}}` — generated by `cargo kick g contributor`.
//!
//! `#[contributor]` rewrites the function below into a unit struct
//! whose name is the function name, plus an `impl ContextContributor`.
//! Register on the parent module's `define()` builder (or directly on
//! `bootstrap()`) via `.contribute({{contributor_pascal}})`.

// The macro rewrites `ContributorRequest` / `KickResult` to absolute
// paths so the imports appear unused at this site. Keep them around for
// editor go-to-definition.
#![allow(unused_imports)]

use kick_rs::{contributor, ContributorRequest, KickResult};

/// Output produced per request by `{{contributor_pascal}}`. Replace
/// this stub with the real shape the rest of the app expects.
#[derive(Debug, Clone)]
pub struct {{contributor_pascal}}Out {
    pub value: String,
}

#[contributor]
pub async fn {{contributor_pascal}}(
    _ctx: &dyn ContributorRequest,
) -> KickResult<{{contributor_pascal}}Out> {
    Ok({{contributor_pascal}}Out {
        value: "{{contributor_snake}} ready".to_owned(),
    })
}
